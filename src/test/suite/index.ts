export function run(testsRoot: string, cb: (error: unknown, failures?: number) => void): void {
    // Create the mocha test
    console.log('🚀 ~ run ~ testsRoot', testsRoot);
    console.log('🚀 ~ run ~ cb', cb);
    console.log('🚀 ~ // TODO test_vsc');
}
